name: mlops-air-quality

# ==============================================================================
# Networks
# ==============================================================================
networks:
  mlops-net:
    driver: bridge

# ==============================================================================
# Persistent Storage Volumes
# ==============================================================================
volumes:
  floci_data:
  grafana_data:
  prometheus_data:
  provisioned_dashboards:

# ==============================================================================
# Services Definition
# ==============================================================================
services:
  # ----------------------------------------------------------------------------
  # 1. Dashboard Downloader (Python-powered to avoid Docker Compose variable syntax traps)
  # ----------------------------------------------------------------------------
  dashboard-init:
    image: python:3.11-alpine
    container_name: local_dashboard_init
    volumes:
      - provisioned_dashboards:/dashboards
    entrypoint: ["python3", "-c"]
    command:
      - |
        import urllib.request, re, os

        url = "https://grafana.com/api/dashboards/25012/revisions/1/download"
        print(f"Downloading dashboard 25012 from {url}...")

        req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})
        with urllib.request.urlopen(req) as resp:
            content = resp.read().decode('utf-8')

        # Clear __inputs array, replace datasource, and aggregate container count queries
        patched = re.sub(r'\"__inputs\"\s*:\s*\[[^\]]*\]', '\"__inputs\": []', content)
        patched = patched.replace('$${DS_PROMETHEUS}', 'Prometheus')
        patched = patched.replace('\"docker_n_containers_running\"', '\"max(docker_n_containers_running)\"')
        patched = patched.replace('\"docker_n_containers_stopped\"', '\"max(docker_n_containers_stopped)\"')

        # Clear old files and write patched dashboard
        out_dir = "/dashboards"
        for f in os.listdir(out_dir):
            os.remove(os.path.join(out_dir, f))

        out_path = os.path.join(out_dir, "docker_telegraf.json")
        with open(out_path, "w") as f:
            f.write(patched)

        print(f"Successfully saved {len(patched)} bytes to {out_path}")

  # ----------------------------------------------------------------------------
  # 2. NGINX Reverse Proxy
  # ----------------------------------------------------------------------------
  nginx:
    image: nginx:1.27.4-alpine
    container_name: local_nginx
    ports:
      - "80:80"
    volumes:
      - ./config/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - mlops-net
    depends_on:
      floci:
        condition: service_healthy
      grafana:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:80/ > /dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M

  # ----------------------------------------------------------------------------
  # 3. Local AWS Emulator (Floci)
  # ----------------------------------------------------------------------------
  floci:
    image: floci/floci:1.6.0
    container_name: local_aws_floci
    ports:
      - "4566:4566"
    environment:
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-test}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-test}
      - PERSISTENCE=1
    volumes:
      - floci_data:/var/lib/floci
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - mlops-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_floci/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G

  # ----------------------------------------------------------------------------
  # 4. Local AWS Web Management Dashboard (Floci UI)
  # ----------------------------------------------------------------------------
  floci-ui:
    image: floci/floci-ui:0.2.0
    container_name: local_floci_ui
    ports:
      - "8080:8080"
    environment:
      - FLOCI_ENDPOINT=http://floci:4566
      - AWS_ENDPOINT_URL=http://floci:4566
      - FLOCI_HOST=floci
      - PORT=8080
      - AWS_REGION=${AWS_DEFAULT_REGION:-us-east-1}
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-test}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-test}
    networks:
      - mlops-net
    depends_on:
      floci:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: 128M

  # ----------------------------------------------------------------------------
  # 5. Container Telemetry Collector (Pure Docker API - Telegraf)
  # ----------------------------------------------------------------------------
  docker-metrics:
    image: telegraf:1.34.0-alpine
    container_name: local_docker_metrics
    user: root
    entrypoint:
      - /bin/sh
      - -c
      - |
        cat <<'EOF' > /etc/telegraf/telegraf.conf
        [agent]
          interval = "5s"
          flush_interval = "5s"

        [[inputs.docker]]
          endpoint = "unix:///var/run/docker.sock"
          gather_services = false
          container_name_include = []
          timeout = "5s"

        [[outputs.prometheus_client]]
          listen = ":9273"
          metric_version = 2
        EOF
        exec telegraf
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - mlops-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9273/metrics"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: 128M

  # ----------------------------------------------------------------------------
  # 6. Host System Metric Exporter (node-exporter)
  # ----------------------------------------------------------------------------
  node-exporter:
    image: prom/node-exporter:v1.9.0
    container_name: local_node_exporter
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'
    networks:
      - mlops-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9100/metrics"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 64M

  # ----------------------------------------------------------------------------
  # 7. Time-Series Database (Prometheus)
  # ----------------------------------------------------------------------------
  prometheus:
    image: prom/prometheus:v3.2.1
    container_name: local_prometheus
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    volumes:
      - ./config/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    networks:
      - mlops-net
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  # ----------------------------------------------------------------------------
  # 8. Telemetry Dashboard (Grafana)
  # ----------------------------------------------------------------------------
  grafana:
    image: grafana/grafana:13.1.3
    container_name: local_grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./config/grafana/datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
      - ./config/grafana/dashboards.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml:ro
      # Reads dynamically generated dashboard from shared volume
      - provisioned_dashboards:/etc/grafana/provisioning/dashboards/json:ro
    networks:
      - mlops-net
    depends_on:
      dashboard-init:
        condition: service_completed_successfully
      prometheus:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M